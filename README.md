# ERC-Platform (Personal Marketing Website)

### Env Variables

Create a .env file in then root and add the following

```
NODE_ENV = production
```

### Install Dependencies (frontend & backend)

```
npm install
cd frontend
npm install
```